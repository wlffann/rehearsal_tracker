<%= form_for [@company, @production, @rehearsal] do |f| %>
  <%= f.label :date %>
  <%= f.text_area :date %>

  <%= f.label :location %>
  <%= f.text_area :location %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit %>
<% end %>
